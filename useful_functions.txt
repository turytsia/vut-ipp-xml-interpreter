define("ERR","value") -- echo ERR;
mb_internal_encoding("UTF-8");

== Arrays ==
\s - white space \S - non-whitespace
count = .length
array_push($target, $value);

== String ==
fgets(STDIN)  reads from the stream by lines
str_replace("what","to_what","where");  returns string with replaced values
strpos($target,"value") Find the position of the first occurrence of a substring in a string, if it's not found - returns false
substr($target, from, to)  cuts substring and returns it
trim  trimming string
strtoupper  makes string UPPERCASE

== Regex ==
"/regex/"
preg_match(regex, string, result_array)  searches by regex preg_match


== Other ==
print_r prints human readable info about variable
isset  check if variable is declared and is diff than null
htmlspecialchars  "<a href='test'>Test</a>" => &lt;a href=&#039;test&#039;&gt;Test&lt;/a&gt;
